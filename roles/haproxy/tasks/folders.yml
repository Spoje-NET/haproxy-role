---
- name: Prepare FullChains directory
  ansible.builtin.file:
    path: /etc/haproxy/ssl/
    state: directory
    mode: '0755'

- name: Ensure ssl HAProxy directory
  ansible.builtin.file:
    path: /etc/haproxy/ssl/
    state: directory
    mode: '0755'
    owner: haproxy
    group: haproxy
